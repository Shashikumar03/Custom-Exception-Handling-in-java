// Defining an interface
public interface Abs {
    // Abstract method declaration (implicitly public and abstract)
    void doSomething();

    // You can also have constants in an interface (implicitly public, static, and final)
    int MAX_VALUE = 100;
}
